{
  "buildCommand": "pnpm install && pnpm --filter @workspace/privelux run build",
  "outputDirectory": "artifacts/privelux/dist/public",
  "installCommand": "echo 'install handled by buildCommand'",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
